---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "border-2 border-border bg-card text-card-foreground shadow-xs",
    className,
  ]}
>
  <slot />
</div>
